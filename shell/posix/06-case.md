# Case Statement

Patternmatching switch statement. Alternative to [Conditional Expressions](05-conditionals).

```sh
case $var in
    pattern1)
        commands
        ;;
    pattern2)
        commands
        ;;
    *)
        default
        ;;
esac
```